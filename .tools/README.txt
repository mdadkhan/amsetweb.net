This directory is generated by the local setup scripts.

Downloaded runtimes, Composer, wrappers, archives, and caches are intentionally
excluded from Git. Run scripts/setup-macos.sh or scripts/setup-windows.ps1 from
any directory to recreate them.
